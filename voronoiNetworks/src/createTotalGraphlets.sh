ls | grep dWL | while read f; do echo "$f"; cat "$f" >> dWL_totalGraphlets.ndump2; done;
ls | grep dWP | while read f; do echo "$f"; cat "$f" >> dWP_totalGraphlets.ndump2; done;
ls | grep cNT | while read f; do echo "$f"; cat "$f" >> cNT_totalGraphlets.ndump2; done;
ls | grep omm | while read f; do echo "$f"; cat "$f" >> eye_totalGraphlets.ndump2; done;
ls | grep Diagrama_20.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_20_totalGraphlets.ndump2; done;
ls | grep BC | while read f; do echo "$f"; cat "$f" >> BCA_totalGraphlets.ndump2; done;
ls | grep Diagrama_1.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_1_totalGraphlets.ndump2; done;
ls | grep Diagrama_2.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_2_totalGraphlets.ndump2; done;
ls | grep Diagrama_3.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_3_totalGraphlets.ndump2; done;
ls | grep Diagrama_4.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_4_totalGraphlets.ndump2; done;
ls | grep Diagrama_5.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_5_totalGraphlets.ndump2; done;
ls | grep Diagrama_6.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_6_totalGraphlets.ndump2; done;
ls | grep Diagrama_7.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_7_totalGraphlets.ndump2; done;
ls | grep Diagrama_8.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_8_totalGraphlets.ndump2; done;
ls | grep Diagrama_9.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_9_totalGraphlets.ndump2; done;
ls | grep Diagrama_10.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_10_totalGraphlets.ndump2; done;
ls | grep Diagrama_11.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_11_totalGraphlets.ndump2; done;
ls | grep Diagrama_12.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_12_totalGraphlets.ndump2; done;
ls | grep Diagrama_13.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_13_totalGraphlets.ndump2; done;
ls | grep Diagrama_14.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_14_totalGraphlets.ndump2; done;
ls | grep Diagrama_15.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_15_totalGraphlets.ndump2; done;
ls | grep Diagrama_16.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_16_totalGraphlets.ndump2; done;
ls | grep Diagrama_17.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_17_totalGraphlets.ndump2; done;
ls | grep Diagrama_18.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_18_totalGraphlets.ndump2; done;
ls | grep Diagrama_19.ndump2 | while read f; do echo "$f"; cat "$f" >> voronoi_19_totalGraphlets.ndump2; done;
